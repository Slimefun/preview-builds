{
  "name": "pre-release",
  "version": "0.0.0",
  "devDependencies": {
    "@blobdevelopment/eslint-config": "^0.0.1",
    "@cloudflare/workers-types": "^4.20230518.0",
    "typescript": "^5.1.3",
    "vitest": "^0.32.2",
    "wrangler": "2.20.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
		"dev": "wrangler dev --local --persist",
    "deploy": "wrangler publish",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^3.2.5"
  }
}
